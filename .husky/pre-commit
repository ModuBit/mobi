set -e
bunx lint-staged
# 循环依赖硬卡：仅当暂存了 src 下文件时跑全项目 depcruise
# （no-circular severity=error，回潮即 exit 非 0 阻止提交；非代码提交跳过避免延迟）
if git diff --cached --name-only | grep -qE '^packages/[^/]+/src/'; then
  bun run lint:deps
fi
